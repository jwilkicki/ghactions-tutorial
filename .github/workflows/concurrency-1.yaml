name: Concurrency 1
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        type: environment
        required: true

concurrency: 
  group: concurrency-1
  cancel-in-progress: true

jobs:
  sleep:
    runs-on: ${{ vars.JOBS_RUNNER }}
    steps:
      - run: sleep 30